export function _method(value: number) :string {
    return "xavi"+value
}

export function fizzBuzz(it: number) :string {
    var res = "";
    for (var i = 1; i <= it; i++) {
        // modulo 3

        // modulo 5

        // modulo 3 y 5

        // caso base
        res += i + "\n";

    }
    return res;
}